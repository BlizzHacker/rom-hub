# Proof matrix

Generated by `scripts/proof_matrix.py`, which runs `rom_hub.importer.run_import` and `rom_hub.metadata.run_enrich` -- the same functions `rom-hub import` and `rom-hub enrich` call -- against a live server of each supported backend. **This file is generated. Re-run the command below rather than editing it.**

* **Produced** 2026-07-30 02:32:28 UTC
* **Command** `python scripts/proof_matrix.py --out docs/PROOF.md --romm-url <url> --romm-user proof --romm-password *** --gaseous-url <url> --gaseous-user proof@proof.invalid --gaseous-password *** --gaseous-platform Unknown --retrom-url <url>`
* **Stack** `scripts/proof-stack.compose.yml` + `scripts/proof-stack-bootstrap.sh` (disposable; torn down after)

Backends it ran against:

| Backend | Version | Image | Endpoint | Declares |
|---|---|---|---|---|
| RomM | 4.9.2 | `—` | `http://<host>:8085` | artwork, collections, firmware, import, metadata, scan |
| Gaseous | 2.0.0.0 | `—` | `http://<host>:8086` | import, scan |
| Retrom | 0.8.4 | `—` | `http://<host>:5102` | artwork, import, metadata, scan |

**Version** is what each server reports about itself, and is the authoritative pin. **Image** is filled in only when the matrix runs on the machine hosting the containers; run from anywhere else there is no Docker daemon to ask, and a blank is better than a guess.

## What the four outcomes mean

| | |
|---|---|
| **PASS** | It ran and the evidence column says what was observed. |
| **FAIL** | The backend declares the capability and it did not work. This is a bug in this project. |
| **UNSUPPORTED** | The backend does not declare the capability *and* the host agreed: `require()` refused before doing any work, or `degrade()` returned a skip and the operation completed without it. Not a bug — and deliberately not spelled the same as FAIL, because conflating the two is how real breakage hides behind an expected gap. |
| **NOT-RUN** | No server, or an earlier step failed so this one was never attempted. Never a stand-in for FAIL. |

## The matrix

| Capability | RomM | Gaseous | Retrom |
|---|---|---|---|
| connect | PASS | PASS | PASS |
| platform lookup | PASS | PASS | PASS |
| list library | PASS | PASS | PASS |
| import | PASS | PASS | PASS |
| post-upload registration | PASS | PASS | PASS |
| dedup on re-import | PASS | PASS | PASS |
| collections | PASS | UNSUPPORTED | UNSUPPORTED |
| metadata write | PASS | UNSUPPORTED | PASS |
| cover art | PASS | UNSUPPORTED | PASS |
| firmware store | PASS | UNSUPPORTED | UNSUPPORTED |

## Evidence

### RomM

| Capability | Outcome | Evidence |
|---|---|---|
| connect | PASS | authenticate() ok against http://<host>:8085; declares artwork, collections, firmware, import, metadata, scan |
| platform lookup | PASS | 'nes' resolved to id 1 |
| list library | PASS | 1 rom(s) before the import |
| import | PASS | job 1 DONE, rom id 2; downloader called for 1 file(s), sha1 3b902b1d52c2 |
| post-upload registration | PASS | scan_platform() ran inside run_import; listing went 1 -> 2 rom(s) |
| dedup on re-import | PASS | second import refused: already in romm (rom-hub-proof-20260730-023228.nes); nothing was uploaded -- matches rom id 2 |
| collections | PASS | declared, and the import reported no collection skip while filing under 'ROM Hub proof matrix' |
| metadata write | PASS | run_enrich wrote name to rom 2 |
| cover art | PASS | 132 bytes of cover attached to rom 2 |
| firmware store | PASS | 1 file(s) uploaded; the backend's own firmware listing for 'nes' now carries rom-hub-proof-20260730-023228.bin |

### Gaseous

| Capability | Outcome | Evidence |
|---|---|---|
| connect | PASS | authenticate() ok against http://<host>:8086; declares import, scan |
| platform lookup | PASS | 'Unknown' resolved to id 0 |
| list library | PASS | 1 rom(s) before the import |
| import | PASS | job 1 DONE, rom id 2; downloader called for 1 file(s), sha1 3b902b1d52c2 |
| post-upload registration | PASS | scan_platform() ran inside run_import; listing went 1 -> 2 rom(s) |
| dedup on re-import | PASS | second import refused: already in gaseous (rom-hub-proof-20260730-023228.nes); nothing was uploaded -- matches rom id 2. adding it to the collection 'ROM Hub proof matrix' was skipped: the 'gaseous' backend does not support collections |
| collections | UNSUPPORTED | not declared; degrade() -> the collection the plan named was skipped: the 'gaseous' backend does not support collections. The import still completed without it |
| metadata write | UNSUPPORTED | require() refuses up front: enriching a rom's metadata needs the 'metadata' capability, which the 'gaseous' backend does not have (it supports: import, scan) |
| cover art | UNSUPPORTED | not declared; degrade() -> the cover art the patch proposed was skipped: the 'gaseous' backend does not support artwork |
| firmware store | UNSUPPORTED | not declared; degrade() -> filing the BIOS in the library was skipped: the 'gaseous' backend does not support firmware |

### Retrom

| Capability | Outcome | Evidence |
|---|---|---|
| connect | PASS | authenticate() ok against http://<host>:5102; declares artwork, import, metadata, scan |
| platform lookup | PASS | 'nes' resolved to id 2 |
| list library | PASS | 2 rom(s) before the import |
| import | PASS | job 1 DONE, rom id 6; downloader called for 1 file(s), sha1 3b902b1d52c2 |
| post-upload registration | PASS | scan_platform() ran inside run_import; listing went 2 -> 3 rom(s) |
| dedup on re-import | PASS | second import refused: already in retrom (rom-hub-proof-20260730-023228.nes); nothing was uploaded -- matches rom id 6. adding it to the collection 'ROM Hub proof matrix' was skipped: the 'retrom' backend does not support collections |
| collections | UNSUPPORTED | not declared; degrade() -> the collection the plan named was skipped: the 'retrom' backend does not support collections. The import still completed without it |
| metadata write | PASS | run_enrich wrote name to rom 6 |
| cover art | PASS | 132 bytes of cover attached to rom 6 |
| firmware store | UNSUPPORTED | not declared; degrade() -> filing the BIOS in the library was skipped: the 'retrom' backend does not support firmware |

## Reproducing this

```sh
docker compose -p proofmatrix -f scripts/proof-stack.compose.yml up -d
bash scripts/proof-stack-bootstrap.sh
python scripts/proof_matrix.py --out docs/PROOF.md --romm-url <url> --romm-user proof --romm-password *** --gaseous-url <url> --gaseous-user proof@proof.invalid --gaseous-password *** --gaseous-platform Unknown --retrom-url <url>
docker compose -p proofmatrix -f scripts/proof-stack.compose.yml down -v
```

Host names are redacted to `<host>`; ports are not, because a port other than the product default is evidence that the run did not touch a real library.
