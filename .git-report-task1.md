# Task 1 Report — FetchPlan types and the `importer` capability contract

Worktree: `C:\MoveWeight\romm-hub-wt-phase2` (branch `feat/phase2-import`)
Date: 2026-07-28

## Summary

Implemented Task 1 of `docs/superpowers/plans/2026-07-28-romm-hub-phase2-import.md` exactly per
the brief: `FetchFile`/`FetchPlan` pydantic models in `types.py`, the `ImportProvider` capability
ABC, SDK exports, and a manifest test proving an `importer` entrypoint parses. TDD followed:
failing test written and confirmed to fail for the stated reason before any implementation code
was added.

## Environment note (pre-existing, not part of Task 1's diff)

Before starting, `python -c "import romm_hub; print(romm_hub.__file__)"` resolved to
`C:\MoveWeight\romm-hub\src\romm_hub\__init__.py` — the OTHER worktree — because the shared
Python user site-packages had an editable install pointing there from earlier work. Per the
plan's Environment Notes ("From the worktree root: `python -m pip install -e ".[dev]"` then
`python -m pytest`"), I ran that install from this worktree root, which repointed the editable
install to `C:\MoveWeight\romm-hub-wt-phase2`. This did not modify any files in
`C:\MoveWeight\romm-hub` — only site-packages metadata (`easy-install.pth` / editable finder) in
the shared Python environment. **Concern:** because both worktrees apparently share one Python
environment (no per-worktree venv), whichever worktree last runs `pip install -e ".[dev]"` wins
the shared editable pointer. If the other agent working in `romm-hub` runs its own install after
this, tests run from this worktree could start resolving `romm_hub` back to the other tree. Worth
each agent double-checking `python -c "import romm_hub; print(romm_hub.__file__)"` before trusting
a pytest run, or moving to per-worktree venvs.

## Files changed

- `src/romm_hub/types.py` — added `FetchFile` (url, filename, size_bytes) and `FetchPlan` (files,
  platform, collection), plus the `_bare_name_only` filename validator (posixpath-based: rejects
  `.`, `..`, any path separator, and anything where `basename(v) != v`).
- `src/romm_hub_sdk/capabilities.py` — added `ImportProvider(Capability)` ABC with abstract
  `plan(self, result: SearchResult) -> FetchPlan`.
- `src/romm_hub_sdk/__init__.py` — exported `ImportProvider`, `FetchFile`, `FetchPlan` alongside
  existing `SearchResult`, `SearchProvider`, `PluginContext`, `HttpResponse`.
- `src/romm_hub/manifest.py` — **not modified**; confirmed `importer` was already present in
  `KNOWN_CAPABILITIES = frozenset({"search", "importer", "metadata", "stream", "cores"})`.
- `tests/test_fetchplan_types.py` — new, verbatim from the brief (10 test cases incl. the
  parametrized `test_filename_must_be_a_bare_name` with 7 evil inputs: `../escape.zip`, `a/b.zip`,
  `a\b.zip`, `/abs.zip`, `..`, `""`, `.`).
- `tests/test_manifest.py` — added `test_importer_entrypoint_parses`, asserting a manifest
  declaring `importer = "archive_org.importer:Importer"` alongside `search` parses and that
  `m.capabilities["importer"]` round-trips correctly.

No other files touched. Nothing under `.git/` committed (only this report file, which is
untracked/not added, sits alongside it per instructions).

## Exact commands and real output

### Baseline (before any change)

```
$ cd C:/MoveWeight/romm-hub-wt-phase2 && python -m pytest -q
....................................s................................... [ 72%]
..........s.....sss........                                              [100%]
94 passed, 5 skipped, 1 deselected in 12.07s
```

### Step 1/2: failing test written, run to confirm the stated failure

First run (before fixing the stale editable install) failed with an `ImportError` sourced from
the WRONG worktree's file (`C:\MoveWeight\romm-hub\src\romm_hub\types.py`), which was itself a
useful early warning of the shared-site-packages issue above:

```
$ python -m pytest tests/test_fetchplan_types.py -v
...
E   ImportError: cannot import name 'FetchFile' from 'romm_hub.types' (C:\MoveWeight\romm-hub\src\romm_hub\types.py)
```

After `python -m pip install -e ".[dev]"` run from this worktree's root (repoints the editable
install; see Environment note above), `python -c "import romm_hub; print(romm_hub.__file__)"`
correctly printed `C:\MoveWeight\romm-hub-wt-phase2\src\romm_hub\__init__.py`, and re-running:

```
$ python -m pytest tests/test_fetchplan_types.py -v
...
E   ImportError: cannot import name 'FetchFile' from 'romm_hub.types' (C:\MoveWeight\romm-hub-wt-phase2\src\romm_hub\types.py)
1 error in 0.25s
```

Confirmed: fails for the exact stated reason (`ImportError: cannot import name 'FetchFile'`), now
correctly attributed to this worktree.

### Step 3/4: implement, then targeted test

```
$ python -m pytest tests/test_fetchplan_types.py -v
tests/test_fetchplan_types.py::test_minimal_plan PASSED                  [ 10%]
tests/test_fetchplan_types.py::test_plan_requires_at_least_one_file PASSED [ 20%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[../escape.zip] PASSED [ 30%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[a/b.zip] PASSED [ 40%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[a\\b.zip] PASSED [ 50%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[/abs.zip] PASSED [ 60%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[..] PASSED [ 70%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[] PASSED [ 80%]
tests/test_fetchplan_types.py::test_filename_must_be_a_bare_name[.] PASSED [ 90%]
tests/test_fetchplan_types.py::test_negative_size_rejected PASSED        [100%]
10 passed in 0.12s
```

Manifest test targeted:

```
$ python -m pytest tests/test_manifest.py -v
... (all 9, including test_importer_entrypoint_parses) PASSED
9 passed in 0.02s
```

Full suite:

```
$ python -m pytest -q
..............................................s......................... [ 65%]
.....................s.....sss........                                   [100%]
105 passed, 5 skipped, 1 deselected in 11.71s
```

105 = 94 (baseline) + 10 (test_fetchplan_types.py) + 1 (test_importer_entrypoint_parses). 5 skips
are the expected Windows `seccomp is Linux-only` skips (unchanged from baseline). 1 deselected
unchanged (pre-existing marker-based deselection, not touched).

### Sanity checks (ad hoc, not part of the pytest suite)

```
$ python -c "from romm_hub_sdk import ImportProvider, FetchFile, FetchPlan, SearchProvider, SearchResult, PluginContext, HttpResponse; print('imports ok')"
imports ok

$ python -c "from romm_hub_sdk import ImportProvider; ImportProvider(ctx=None)"
TypeError: Can't instantiate abstract class ImportProvider without an implementation for abstract method 'plan'
```

Confirms `ImportProvider` is a true ABC — a plugin cannot subclass it without providing `plan`.

### Post-commit verification

```
$ python -m pytest -q
105 passed, 5 skipped, 1 deselected in 11.66s

$ git status --short
(clean)
```

## Commit

```
$ git add src/romm_hub/types.py src/romm_hub_sdk/__init__.py src/romm_hub_sdk/capabilities.py tests/test_manifest.py tests/test_fetchplan_types.py
$ git commit -m "feat(import): FetchPlan types and the ImportProvider capability"
[feat/phase2-import 98986ab] feat(import): FetchPlan types and the ImportProvider capability
 5 files changed, 87 insertions(+), 5 deletions(-)
 create mode 100644 tests/test_fetchplan_types.py
```

SHA: `98986ab`

## Global Constraints check

- Plugin never holds the RomM token / never uploads — N/A to this task (no host/upload code
  touched); `ImportProvider.plan` only returns a `FetchPlan` description, never performs I/O.
- URL allowlist gating (`netpolicy.check_url`) is explicitly Task 2's job. Nothing added here
  makes that harder: `FetchPlan`/`FetchFile` are plain data (pydantic models), no bypass hooks,
  no eager fetching, no caching of URLs outside the model itself.
- `src/romm_hub/sandbox.py` and its seccomp denylist — untouched.
- No test in this task calls `sandbox.install()`; nothing runs in a child process because nothing
  here needed to.
- `netpolicy`, fail-closed sandbox policy, `ROMM_HUB_ALLOW_UNSANDBOXED` — untouched.
- No downloads performed by this task (no fetch code yet) — `var/downloads/` not created or used.
- No RomM network calls in this task at all (no client code yet).
- Python 3.12, `src/` layout, TDD followed (test-first, confirmed-fail, then implement).
- Ran on Windows only (this session); expected 5 skips (`seccomp is Linux-only`) present and
  unchanged from baseline. Linux verification is out of scope for Task 1 per the plan (it's a
  Task 8 whole-suite concern) but nothing in this diff is platform-specific — it's pure pydantic
  models and an ABC method signature.

## Concerns

1. **Shared editable install across worktrees** (see Environment note above) — the two worktrees
   apparently share one Python site-packages, not isolated venvs. Running `pip install -e ".[dev]"`
   in this worktree repointed the shared editable install here, which could invalidate the other
   agent's `romm-hub` install if they haven't re-run their own install since. This is inherent to
   the shared-environment setup, not something this task's code changes caused, but it is a live
   risk for concurrent work in the two worktrees. No git state in either repo was touched by this.
2. Everything else is a straight, low-risk implementation exactly matching the brief's given code
   listings verbatim (test file, `FetchFile`/`FetchPlan`, `ImportProvider`). No deviations.
