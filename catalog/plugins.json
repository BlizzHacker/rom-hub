{
  "catalog_version": "1",
  "updated": "2026-07-29",
  "plugins": [
    {
      "slug": "aminet",
      "name": "Aminet (Amiga)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-aminet",
      "install": "https://github.com/BlizzHacker/rom-hub-aminet",
      "download": "https://github.com/BlizzHacker/rom-hub-aminet/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer"
      ],
      "network": [
        "aminet.net"
      ],
      "status": "caveat",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Searches Aminet's 6,700-package game tree for Amiga games and imports the archive directly.",
      "terms": "Freely distributable by admission rule, which is about as good as an archive's terms get. Aminet's uploading instructions open with “This site is intended for the distribution of any type of freely distributable software” and then refuse, first on the list, “Unlicensed copies of commercial software” and “Software with a license in conflict with Aminet's nature”. Freely distributable is not a description of the archive, it is the condition of being in it, moderated since 1992. What you reach is public-domain, freeware, shareware and open-source Amiga software its authors uploaded themselves; each package's own terms are in its `.readme`, which this plugin fetches on every import anyway. `game/demo` is “Demos of commercial games” and belongs here for the same reason the rest does — a publisher's playable demo was published *for* free distribution. `aminet.net` serves no robots.txt (its /robots.txt is the site's themed 404 page), so there is no crawl directive to observe.",
      "comments": "The caveat is the architecture, and it is the whole plugin. Aminet is not an Amiga archive — it is the archive for every system that grew out of the Amiga, and one `game/think` directory holds `abrick.lha` (AmigaOS 4 on PowerPC), `abandoned_bricks-mos.lha` (MorphOS) and `alleytris_68k.lha` (AmigaOS on 68k) under near-identical names. RomM has `amiga` and `amiga` means the Commodore machine, so exactly three architectures map — `m68k-amigaos`, plus `ppc-warpup` and `ppc-powerup`, which are accelerator cards *inside* an Amiga — and MorphOS, AmigaOS 4, AROS and Amithlon each refuse **by name**, saying which computer they actually are. Measured over a live `dir=game` search: 98 of 172 rows were `m68k-amigaos`. The rest still appear in search with no platform set, because hiding a game somebody can see on Aminet's own site would be worse than showing why it will not import. Four of the eighteen `game/` shelves — `data`, `edit`, `hint`, `patch` — hold data files, level editors, walkthroughs and patches; they are hidden by default and never import. Two traps for the next person: light and dark result rows are **different markup**, so a regex anchored on the opening tag finds exactly half the results; and this host answers a missing path with HTTP **200** and an error body, so both parsers refuse a document that is not the shape they expect rather than reading zero rows out of it."
    },
    {
      "slug": "archive-org",
      "name": "Archive.org",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-archive-org",
      "install": "https://github.com/BlizzHacker/rom-hub-archive-org",
      "download": "https://github.com/BlizzHacker/rom-hub-archive-org/archive/refs/tags/v0.2.0.tar.gz",
      "version": "0.2.0",
      "ref": "v0.2.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer",
        "metadata",
        "stream"
      ],
      "network": [
        "archive.org",
        "*.archive.org"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Searches the Internet Archive's software collections by title and imports the item's payload file.",
      "terms": "Mixed, and the Archive says which is which. The default `softwarelibrary` scope holds public-domain and abandonware software alongside commercial titles still under copyright — the Internet Archive hosts them under its own library and DMCA position, not under a licence that passes to you. Items the Archive will only let you play in a browser are marked `stream_only`, and the importer refuses those outright rather than working around the restriction. Access is public and unauthenticated; `/advancedsearch.php`, `/metadata/` and `/download/` are not disallowed by the Archive's robots.txt.",
      "comments": "Results carry `extra.stream_only` so a UI can route an item to streaming instead of offering an import that would be refused. `metadata.emulator` maps to a RomM platform through an exact-match table with no fallback: an unmapped emulator fails loudly as \"needs mapping\" rather than filing a ROM under a guessed system. Search terms are confined to the title field — a bare term used to go to Archive.org's default field, which returned `Die Hard` for `sonic`."
    },
    {
      "slug": "demozoo",
      "name": "Demozoo (demoscene)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-demozoo",
      "install": "https://github.com/BlizzHacker/rom-hub-demozoo",
      "download": "https://github.com/BlizzHacker/rom-hub-demozoo/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer"
      ],
      "network": [
        "demozoo.org",
        "files.scene.org",
        "archive.scene.org",
        "fujiology.org"
      ],
      "status": "caveat",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Searches Demozoo's 386,682-production demoscene catalogue for demos, intros and cracktros and imports them from scene.org.",
      "terms": "Demoscene productions are released free by their authors for distribution — that is what the form is. A demo exists to be copied, and scene.org's entire purpose is copying them; the archives this plugin reaches are the ones the scene runs for itself. That is not a licence granted to anybody, and many productions carry no stated terms at all, but free circulation has been the condition of the whole culture since the 1980s. Cracktros are the one class where the original context was piracy: the intro itself is the group's own code and artwork, which is why it is the part that got archived, and the game it was attached to is not here. On robots: `demozoo.org/robots.txt` disallows `/search/`, `/account/` and `/beep-boop/` with `Crawl-delay: 10`, and this plugin uses only `/api/v1/`, which is not restricted — Demozoo's substring search lives behind that disallowed `/search/` and is therefore not used, which is why title matching is exact. `files.scene.org` and `fujiology.org` serve no robots.txt. Three archives Demozoo links to are refused outright: `csdb.dk` (its robots.txt is `Disallow: /` for ClaudeBot and anthropic-ai, and disallows `/release/download.php` for everyone), `ftp.amigascne.org` (`Disallow: /` for every user agent) and `files.zxdemo.org` (403 on its own root, unverifiable). csdb.dk alone would roughly double C64 coverage and it stays out.",
      "comments": "Two traps, and both are the whole plugin. **The API silently ignores unknown parameters**: `?search=`, `?q=`, `?title__icontains=` and `?page_size=` all return HTTP 200 and the complete unfiltered 386,682-row listing, so a plugin that hopefully sent one would look like it was searching while handing back the alphabetical head of the database. Only `title`, `platform`, `production_type`, `supertype` and `page` are real, and `title` is an **exact** case-insensitive match — `title=crackt` returns zero. **And Demozoo hosts nothing**: `files.scene.org/get/<p>` 302s to `http://http.us.scene.org/...`, plain http, which `netpolicy` refuses outright whatever the allowlist says. The plugin pins `get:nl-https`, which resolves to exactly one https host, `archive.scene.org` — declared, because every redirect hop is re-checked. `Video` is the filter's reason for existing: its supertype is `production`, exactly like `Demo`, so a supertype-only rule keeps it, and its download links are usually a YouTube page. The whole `graphics` and `music` supertypes are out too, deliberately — `Executable Graphics` really is an executable, and forty thousand single-image entries beside the games would ruin the shelf. Platform absences come in three flavours with three different sentences: **needs mapping** for a real machine (`Acorn Archimedes`, `BBC Micro`), **not a library platform** for `Windows`/`Linux`/`Browser`/`Paper`, and **ambiguous** for `Neo Geo`, `Neo Geo Pocket / NGPC` and `Thomson`, each of which is one Demozoo platform over two RomM ones. Filename sanitising deliberately does **not** collapse runs of underscores: scene.org really does serve `2.8k_nuance__onslaught.zip`."
    },
    {
      "slug": "emulators",
      "name": "Standalone emulators (GitHub releases)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-emulators",
      "install": "https://github.com/BlizzHacker/rom-hub-emulators",
      "download": "https://github.com/BlizzHacker/rom-hub-emulators/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "cores"
      ],
      "network": [
        "api.github.com",
        "github.com",
        "release-assets.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Lists and installs standalone emulator builds — DuckStation, mGBA, PCSX2, melonDS — from each project's own GitHub releases.",
      "terms": "Four projects, four different answers, and the plugin prints each one in the listing rather than burying it here: mGBA is MPL-2.0, PCSX2 and melonDS are GPL-3.0, and **DuckStation is not open source at all** — it relicensed to CC BY-NC-ND 4.0, which permits non-commercial redistribution of the unmodified build and forbids derivatives. GitHub's API reports DuckStation's SPDX id as `NOASSERTION`, so the value was read from its `LICENSE` file rather than the SPDX field. Nothing here redistributes anything: the plugin names the project's own release URL and the Hub fetches it once, for the operator who asked, which is the same act as clicking the asset on the release page. `api.github.com` serves no robots.txt; `github.com`'s robots.txt disallows `/*/download` to crawlers, and nothing here crawls — one named asset, once, on an explicit `cores install`, following no links and indexing nothing. No emulator here ships or requires firmware from this plugin.",
      "comments": "The second plugin exercising `cores`, and the argument for using it rather than inventing a capability is in the README: `CoreArtifact` already carries every field a standalone emulator needs, the host already installs into `<cores_dir>/<slug>/`, and a new capability would duplicate all of `cores` to express a `.so`-versus-`.AppImage` distinction nothing downstream acts on. Deliberately **not** the `assets` capability: a shader is data an emulator reads, this is the emulator. Asset selection is an anchored regex per (project, target) that must match **exactly one** file — upstream naming is four conventions, not one (`DuckStation-x64.AppImage`, `mGBA-0.10.5-appimage-x64.appimage`, `pcsx2-v2.6.3-linux-appimage-x64-Qt.AppImage`, `melonDS-1.1-appimage-x86_64.zip`), and any rule loose enough to cover those also covers a `.flatpak`, a `-symbols.7z` and an `-installer.exe`. Zero matches and two matches both refuse. mGBA has **no** macOS target on purpose: 0.10.5 ships both `-macos.dmg` and `-osx.dmg` and says nowhere which macOS or which architecture either is for. **Dolphin is absent and it is not an oversight** — it publishes no GitHub releases (`/releases/latest` answers 404) and its own site sits behind a bunny.net JavaScript challenge that 403s even `/robots.txt`, which a socket-less plugin cannot and should not get past. Release assets 302 to `release-assets.githubusercontent.com` — exactly one hop — which is declared; `objects.githubusercontent.com` is deliberately not, because it is not in today's chain."
    },
    {
      "slug": "hasheous",
      "name": "Hasheous",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-hasheous",
      "install": "https://github.com/BlizzHacker/rom-hub-hasheous",
      "download": "https://github.com/BlizzHacker/rom-hub-hasheous/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "metadata"
      ],
      "network": [
        "hasheous.org"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Turns a ROM's hash into a game identity, and hands back other providers' ids — IGDB, TheGamesDB, RetroAchievements — without you holding a key for any of them.",
      "terms": "The cleanest terms of any identification source here, because there is no game content in an answer. Hasheous distributes matching data only: hash to name to other databases' identifiers. Its own README says it \"Is completely free to use\" and its MCP documentation says the hosted endpoint is \"intentionally public for free database lookups\"; the lookup routes take no key and this plugin sends no credential. The DAT catalogues it ingests (No-Intro, Redump, TOSEC, MAME) are published openly by those projects, and the metadata ids it maps to belong to IGDB, TheGamesDB and RetroAchievements — hasheous holds those keys so you do not have to. Its robots.txt allows `User-agent: *` and disallows some named AI crawlers; this plugin is a ROM manager issuing one GET per hash, not a crawler.",
      "comments": "Metadata only, and hash-keyed only: the four `ByHash` routes are the whole GET surface, so a ROM with no hash is refused rather than resolved by searching for its title and taking the top hit. Pass the digest with `--source-id md5:<hex>` (or sha1:, sha256:, crc:, or a bare digest — 8/32/40/64 characters are unambiguous). CRC-32 is refused unless `allow_crc32` is set, because 32 bits over this corpus collide and the failure is a confident answer about a different game. Every answer is cross-checked against the console RomM already has the ROM filed under, comparing hasheous's own DAT-derived `signature.game.system` first; a mismatch refuses and writes nothing. Only provider ids hasheous itself marks `Mapped` are written — a `NotMapped` row is a scheduled search, not an answer."
    },
    {
      "slug": "homebrew",
      "name": "Homebrew Hub (gbdev)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-homebrew",
      "install": "https://github.com/BlizzHacker/rom-hub-homebrew",
      "download": "https://github.com/BlizzHacker/rom-hub-homebrew/archive/refs/tags/v0.2.0.tar.gz",
      "version": "0.2.0",
      "ref": "v0.2.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer",
        "metadata"
      ],
      "network": [
        "hh3.gbdev.io"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Searches gbdev's Homebrew Hub for Game Boy / Game Boy Color homebrew, imports the ROM directly, and proposes the submitter's own title and cover.",
      "terms": "The cleanest source in this directory. Homebrew Hub indexes games written by their authors for the Game Boy, published by those authors for free distribution — the rights holder is the uploader, so there is no third-party copyright being routed around. Individual entries carry their own licences, which vary; the Hub's own catalogue metadata is community-maintained and openly published. Nothing here is a commercial ROM.",
      "comments": "The only plugin in this directory built from the start for material that is unambiguously free to redistribute. One host does both jobs — `/api/search` answers queries and `/static/` serves the ROMs with no redirect off it — so the allowlist is a single host. `hh.gbdev.io`, the human-facing site those results link to, is deliberately not declared, because the plugin shows that URL and never fetches it. `metadata` proposes the Hub's title and the entry's `cover.*` image, and nothing else. It may write a title where libretro-thumbnails may not, because for homebrew the submitter *is* the publisher of record — but only a file actually named `cover.*` becomes artwork, since roughly half the entries carry in-game screenshots instead and promoting one would fill a library with gameplay stills. Resolution is exact or it refuses: three live entries are titled exactly \"Snake\", and a query landing on those names all three rather than choosing."
    },
    {
      "slug": "itch-io",
      "name": "itch.io (free games) — NO IMPORT",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-itch-io",
      "install": "https://github.com/BlizzHacker/rom-hub-itch-io",
      "download": "https://github.com/BlizzHacker/rom-hub-itch-io/archive/refs/tags/v0.3.0.tar.gz",
      "version": "0.3.0",
      "ref": "v0.3.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer",
        "metadata"
      ],
      "network": [
        "itch.io",
        "*.itch.io",
        "img.itch.zone"
      ],
      "status": "caveat",
      "search_only": true,
      "key_required": false,
      "in_tree": false,
      "description": "Finds free games on itch.io and proposes the developer's own title and cover art. It cannot import them, and never will as built — every import is refused by design.",
      "terms": "Consent by construction, within a boundary this plugin does not cross. itch.io is a storefront where the uploader is the rights holder, so anything listed free is offered free by the person entitled to offer it. But itch.io's robots.txt `Disallow`s `/search` and `/game/download/`, and its download URLs are issued only for a POST carrying the game page's csrf_token. This plugin reads only `/games/...` browse listings, which the same robots.txt permits, and it authenticates as nobody. Note that itch.io's \"free\" filter includes name-your-own-price titles; those are refused. The `metadata` capability reads only the public game page, which the same robots.txt permits, and the cover it names is the one itch.io itself serves as that page's og:image.",
      "comments": "The declared `importer` capability always refuses. That is the accurate answer for this source, not an unfinished feature: the alternative was planning a URL that answers 302 with an HTML page, which the host would then hash, upload and file in RomM as a ROM. The refusal names the reason — csrf_token, GET-only broker, robots.txt — so it does not read as a defect. If the broker ever grows a POST verb, exactly one branch changes. `metadata` is what makes it worth installing: it cannot fetch the game, but it reads the developer's own title and cover off the game page, which is what a library is missing for a title it already has. `img.itch.zone` joins the allowlist for that alone — the host fetches the cover this plugin names, so an undeclared image host would fail every enrich as a policy violation. There is no lookup by name, deliberately: robots.txt disallows /search and the browse listings are a small popularity-ordered slice, so hunting one title through them would attach the wrong developer's cover more often than the right one. Pass --source-id."
    },
    {
      "slug": "libretro-cheats",
      "name": "libretro cheat files",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-libretro-cheats",
      "install": "https://github.com/BlizzHacker/rom-hub-libretro-cheats",
      "download": "https://github.com/BlizzHacker/rom-hub-libretro-cheats/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "assets"
      ],
      "network": [
        "api.github.com",
        "raw.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "RetroArch cheat files from libretro-database's cht/ tree, for the systems you choose.",
      "terms": "CC-BY-SA-4.0, verified by reading libretro-database's own LICENSE rather than a badge — the full Creative Commons Attribution-ShareAlike 4.0 text sits at the repository root and GitHub's detection agrees. ShareAlike is the live obligation: a redistributed derivative has to carry the same terms, which is the difference from the overlays plugin's plain CC-BY. One caveat is stated in the plugin's README rather than glossed: libretro-database's own README notes that much of the repository is imported from No-Intro, Redump, TOSEC and GameTDB without saying which upstream terms attach to which subtree. That caveat is about the DAT and metadata imports (dat/, metadat/, rdb/); this plugin touches only cht/, which is contributed directly and which the repository-level LICENSE covers with no carve-out naming it. Both hosts are public and unauthenticated and this plugin sends no credential.",
      "comments": "The source that makes the size problem real: libretro-database is 795 MB and its cht/ tree alone runs to tens of thousands of files across 44 systems. Nothing is cloned — a catalogue is one Git Trees API call per selected system and an install is one file of a few hundred bytes. The trap this plugin was built around is that GitHub's contents API truncates a directory listing at 1,000 entries with no error and no flag, so a contents-API implementation would have offered 1,000 of the NES tree's 2,265 files and looked like it was working; the Trees API returns all of them, sets a `truncated` boolean when it cannot, and is smaller on the wire because it carries no per-entry URL block. A truncated listing is refused outright rather than shown as though it were complete. Because every system at once is far past the 512 assets a plugin may return, the first run makes the one cheap call that enumerates the systems and names them in the refusal — an empty catalogue would have been technically true and useless."
    },
    {
      "slug": "libretro-content",
      "name": "libretro content (buildbot)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-libretro-content",
      "install": "https://github.com/BlizzHacker/rom-hub-libretro-content",
      "download": "https://github.com/BlizzHacker/rom-hub-libretro-content/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer"
      ],
      "network": [
        "buildbot.libretro.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Searches and imports the free game content libretro distributes for RetroArch — homebrew, demos and openly licensed ROMs across 29 RomM platforms.",
      "terms": "Content libretro publishes for RetroArch to download, used the way it is meant to be used. These directories are the back end of RetroArch's own *Load Content → Download Content* menu, served unauthenticated so that software reads them. What is in them is homebrew and demos their authors gave libretro for distribution (`Alter Ego`, `Chrono Knight`, `Sheep It Up`), openly licensed technical software (`240p Test Suite`, MIT), and free game data for engine cores (Cave Story's freeware release, the Quake shareware episode). One directory needs its own sentence: the GCE Vectrex library is **not** public domain — Smith Engineering, which designed the Vectrex, granted permission in 1992 for its ROMs, manuals and overlays to be copied and distributed **as long as it is not for profit**. That is a real grant from the actual rights holder and also a condition: free to acquire and keep, not free to sell. `buildbot.libretro.com/robots.txt` carries only content-signal declarations about AI training and search indexing and `Disallow`s nothing.",
      "comments": "The broadest free-content source in this directory: 29 RomM platforms against `homebrew`'s four. There is no `.index-extended` here — the sibling `libretro-cores` plugin reads one for cores and the content tree has none (checked live: HTTP 404) — so the h5ai listing *is* the catalogue, and this plugin reads the plain `<table>` fallback h5ai ships for clients that run no JavaScript. The platform table is keyed on the directory names **this** server serves, not copied from the libretro thumbnail server, which spells two of the same machines differently (`Coleco - Colecovision` here against `Coleco - ColecoVision` there; `Nintendo - GameBoy` against `Nintendo - Game Boy`) — copying would have produced two silent 404s. An unmapped directory refuses with one of three different sentences, because “add a row” is right for only one of them: **needs mapping** for a real RomM platform, **not a platform** for `Images`/`Video`/`Utilities`, and **ambiguous** for `Nintendo - GameCube - Wii`, which is one directory holding two consoles RomM keeps separate. Search is one request when `--platform` is given and bounded by `max_systems` when it is not, because each listing is its own round trip against a 30-second host timeout. h5ai's size column is rounded (`40 KB`) and there is no byte count in the document, so it is carried as display text and never as `size_bytes`."
    },
    {
      "slug": "libretro-cores",
      "name": "libretro cores (buildbot)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-libretro-cores",
      "install": "https://github.com/BlizzHacker/rom-hub-libretro-cores",
      "download": "https://github.com/BlizzHacker/rom-hub-libretro-cores/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "cores"
      ],
      "network": [
        "buildbot.libretro.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Lists and installs emulator cores from libretro's public buildbot, for a build target you choose.",
      "terms": "A public distribution point used the way it is meant to be used. libretro's buildbot serves the cores libretro itself builds, unauthenticated, and its `.index-extended` catalogue exists so that software can read it — RetroArch's own core updater is the reference consumer. Nothing is circumvented; `buildbot.libretro.com/robots.txt` carries only content-signal declarations about AI training and search indexing and `Disallow`s nothing. The cores themselves are other people's software under their own licences — mostly GPL/LGPL, some more restrictive — and each core's licence travels with the core, not with this plugin.",
      "comments": "The only plugin here exercising `cores`, which is what shows that capability is a real contract rather than a line in a spec. The build target is config and is never detected from the host OS: the Hub is routinely not running on the machine that will load these cores, and a Linux `.so` handed to a Windows frontend fails silently weeks later. An unknown target is refused by name. `system` is filled from a hand-kept table and is blank when unknown — libretro publishes that mapping only inside a zip, and `ctx.http` returns text, so a blank means \"not known\" rather than a guess. The crc32 the index prints is deliberately not verified: the plugin never sees the bytes, the host fetches them, and claiming a check that does not happen would be worse than silence."
    },
    {
      "slug": "libretro-database",
      "name": "libretro Database (DAT)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-libretro-database",
      "install": "https://github.com/BlizzHacker/rom-hub-libretro-database",
      "download": "https://github.com/BlizzHacker/rom-hub-libretro-database/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "metadata"
      ],
      "network": [
        "raw.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Resolves a ROM to its No-Intro or Redump catalogue entry by hash and proposes that entry's name.",
      "terms": "About as clean as a metadata source gets. `libretro/libretro-database` is a public repository whose DAT files exist to be read by programs like this one — it is what RetroArch itself does to build playlists. A DAT is a list of names, sizes and checksums: there is no game content in it, so no copyright is being routed around. The catalogues underneath are No-Intro's and Redump's, published openly by those projects and republished by libretro. `raw.githubusercontent.com` serves no robots.txt at all (HTTP 404), so there is no crawl directive to observe; the plugin fetches at most one file per configured set per ROM regardless.",
      "comments": "Writes the DAT's `game (name …)` and **never** its `rom (name …)`. Those differ in the real data — the Game Boy game titled `14 Juillet (World) (Fr)` has a rom file named `14 Juillet (World) (Fr) (Aftermarket) (Unl).gb` — and the parser keeps them in separate attributes so they cannot be confused, which is the mistake libretro-thumbnails documented rather than made. Matching is by CRC-32/MD5/SHA-1 first and by an exact filename second; a hash that misses does **not** fall back to the filename, because you named a specific dump. Sets no `libretro_id`: RomM's own handler defines that field as the SHA-1 of a libretro *thumbnail* filename, so a DAT-derived value there would collide with a field RomM maintains for something else. One DAT is fetched per lookup and no caching is possible, so note the headroom: `redump/Sony - PlayStation 2.dat` is 4,060,828 bytes against the Hub's 4,194,304-byte response ceiling — about 3% — and a size refusal is reported as exactly that rather than as a miss. Arcade, DOS and the 8-bit micros are absent: those catalogues live under different directories with different keys."
    },
    {
      "slug": "libretro-overlays",
      "name": "libretro overlays (bezels and gamepads)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-libretro-overlays",
      "install": "https://github.com/BlizzHacker/rom-hub-libretro-overlays",
      "download": "https://github.com/BlizzHacker/rom-hub-libretro-overlays/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "assets"
      ],
      "network": [
        "api.github.com",
        "raw.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "RetroArch bezels and on-screen gamepad overlays from libretro's common-overlays, CC-BY-4.0.",
      "terms": "CC-BY-4.0, verified by reading common-overlays' own COPYING — the full Creative Commons Attribution 4.0 text — rather than taking GitHub's badge for it, though GitHub's detection agrees (SPDX CC-BY-4.0). Attribution is the live obligation and it travels with the overlay, not with this plugin: the Hub does not add attribution to files it downloads, so anyone redistributing one carries that themselves. Both hosts are public and unauthenticated and this plugin sends no credential.",
      "comments": "The plugin that had to say no to most of its own source, and the interesting entry in this set for that reason. A RetroArch overlay is a .cfg plus the images it references, and the .cfg names them relative to itself — overwhelmingly as a subdirectory (`img/dpad-left.png`). A FetchPlan cannot express that: every filename the host writes must be a bare name, which is the rule that keeps a plugin's downloads inside the directory chosen for them, and widening it to suit a file layout would trade a containment guarantee for a convenience. So the catalogue is filtered to the self-contained overlays only — 49 of the repository's 310 .cfg files, including the whole gamepads/lite/ pack — because listing an overlay that would fail to install is worse than not listing it. Making that filter cheap is the other half: reading 310 .cfg bodies would be 310 requests for a catalogue, so the tree itself is the predictor (an overlay is self-contained exactly when its own directory also holds images), and that heuristic was checked against the content of all 310 files with no false positives and no false negatives. plan() still fetches the chosen .cfg and re-reads its references, so the heuristic decides what to offer and the file decides what to install."
    },
    {
      "slug": "libretro-thumbnails",
      "name": "libretro Thumbnails",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-libretro-thumbnails",
      "install": "https://github.com/BlizzHacker/rom-hub-libretro-thumbnails",
      "download": "https://github.com/BlizzHacker/rom-hub-libretro-thumbnails/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "metadata"
      ],
      "network": [
        "thumbnails.libretro.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Proposes box art, title screens and in-game screenshots for a ROM by matching its No-Intro-style name.",
      "terms": "Read the licence before you publish anything downstream. The libretro thumbnail packs are community-collected scans and captures of commercial game packaging and screens; the artwork is the publishers' copyright, and libretro distributes it for use in emulator frontends rather than under a licence that grants redistribution. Fetching it to illustrate your own library is the intended use. The host is public and unauthenticated, and its robots.txt does not disallow the image paths.",
      "comments": "Metadata only — it proposes art and never touches a ROM. Matching is by exact No-Intro-style filename against the per-system index, so a name this plugin cannot spell yields no art rather than the wrong game's box. One host, verified to serve images with zero redirects, so there is no CDN hop to declare."
    },
    {
      "slug": "nointro-archive",
      "name": "No-Intro sets on Archive.org",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-nointro-archive",
      "install": "https://github.com/BlizzHacker/rom-hub-nointro-archive",
      "download": "https://github.com/BlizzHacker/rom-hub-nointro-archive/archive/refs/tags/v0.2.1.tar.gz",
      "version": "0.2.1",
      "ref": "v0.2.1",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer"
      ],
      "network": [
        "archive.org",
        "*.archive.org"
      ],
      "status": "caveat",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Reads plain HTTP directory indexes of No-Intro sets held on the Internet Archive, matching by file name.",
      "terms": "Plainly: these are copyrighted commercial console ROMs, and this plugin does not launder that. No-Intro sets are checksum-verified dumps of retail cartridges; the copyright belongs to the publishers, most of whom have never licensed redistribution. Whether you may download one depends on where you live and on whether you own the original media — in the United States the archival exemption courts have recognised does not extend to downloading a copy of something you do not own. The plugin circumvents no access control, paywall, login or robots directive, and `https://archive.org/download/` is a public unauthenticated listing the Archive's robots.txt does not disallow. If you want only material that is unambiguously free to redistribute, use `homebrew` instead.",
      "comments": "Formerly `myrient`. Myrient (myrient.erista.me) shut down on 31 March 2026 and now answers HTTP 200 with the same 2,334-byte notice for every path — including paths it never served — so status codes cannot detect it. The plugin therefore checks that a page actually parsed as an index, which is the only thing standing between a dead source and an HTML page filed as a ROM. Its Myrient index parser and Wayback-captured fixture are retained in case a mirror reproduces that layout. MiNERVA, the successor, `Disallow`s `/browse/` and `/rom/` and is deliberately not used."
    },
    {
      "slug": "open-bios",
      "name": "Open BIOS",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-open-bios",
      "install": "https://github.com/BlizzHacker/rom-hub-open-bios",
      "download": "https://github.com/BlizzHacker/rom-hub-open-bios/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "firmware"
      ],
      "network": [
        "raw.githubusercontent.com",
        "github.com",
        "release-assets.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Clean-room, openly licensed BIOS replacements — Game Boy, Game Boy Color and Game Boy Advance — with each item's licence printed before you install it.",
      "terms": "The only plugin here whose whole subject is what you are permitted to have, so nothing in it is a dump of a retail chip. Cult-of-GBA's `bios.bin` is MIT: its LICENSE is the Expat text, \"Copyright 2020 - 2021 DenSinH and fleroviux\", and GitHub reports the repository as MIT. SameBoy's boot ROMs are MIT (Expat) and that needed checking rather than reading off a badge — GitHub reports the repository as NOASSERTION because its LICENSE excepts the iOS and HexFiend directories, and `BootROMs/` is neither, so the boot ROMs are covered and the release archive ships that same LICENSE beside them. Both hosts are public and unauthenticated and this plugin sends no credential; it makes no request of its own at all, and the Hub makes one GET per install. The Hub cannot verify any of this — a dumped BIOS and a reimplemented one look identical on the wire — so it is a rule about what the catalogue may contain, enforced by review of `open_bios/catalogue.py`, where every claim sits beside its evidence.",
      "comments": "The only plugin exercising `firmware`, and the first to use its archive support: SameBoy publishes no standalone boot-ROM asset, so the plugin declares `archive = \"zip\"` plus the members it wants and the host keeps exactly those, discarding the 1.6 MB emulator build they arrive in. `list()` opens no socket — the catalogue is static and pinned, because firmware is bytes an emulator executes and an operator should get the release the README says was verified rather than whatever was tagged this morning; `sameboy_release` is the escape hatch. Platforms come from an exact-match table with no fallback, which is stricter than the ROM-side equivalent on purpose: a ROM under the wrong system is visibly wrong, while a BIOS under the wrong system is invisible — the emulator just keeps reporting it has none. Four real candidates were left out and `catalogue.py` says why each: PCSX-ReDux OpenBIOS (no releases and no tags at all, so there is no binary to fetch), nds-bootstrap (a homebrew loader, not a BIOS), C-BIOS (source only), and SameBoy's Super Game Boy boot ROMs (no unambiguous platform for a SNES peripheral, and this plugin never guesses one)."
    },
    {
      "slug": "openvgdb",
      "name": "OpenVGDB",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-openvgdb",
      "install": "https://github.com/BlizzHacker/rom-hub-openvgdb",
      "download": "https://github.com/BlizzHacker/rom-hub-openvgdb/archive/refs/tags/v0.2.0.tar.gz",
      "version": "0.2.0",
      "ref": "v0.2.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "metadata"
      ],
      "network": [
        "raw.githubusercontent.com",
        "gamefaqs.gamespot.com",
        "github.com",
        "release-assets.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Proposes a curated title — `Tetris`, not `Tetris (World) (Rev A)` — and a cover, from the Open Video Game Database, which the Hub fetches and verifies for it.",
      "terms": "Read this one carefully, because the honest answer is in two halves. OpenVGDB is published as a public GitHub release and is bundled or downloaded by a long list of emulator frontends, OpenEmu and Provenance among them — but its repository declares no licence file and GitHub reports no licence for it, so \"freely redistributable in practice\" is accurate where \"licensed for redistribution\" would not be. The hash and filename tables are catalogue facts about dumps. The cover art is not in the database at all: it stores URLs to GameFAQs and to community projects, and the images at the other end are the publishers' copyright, hosted rather than licensed to you. Fetching one to illustrate your own library is what the database is for; republishing a library built that way is your call. `art.gametdb.com` covers are never proposed, because that host's robots.txt disallows everything.",
      "comments": "**Install and go.** OpenVGDB publishes no API — the whole project is one 8.7 MB release asset — so this plugin declares that file as a data asset in its manifest and the Hub fetches it: the redirect to `release-assets.githubusercontent.com` is re-validated hop by hop, the one declared member is unpacked, the 40.3 MiB `openvgdb.sqlite` is checked against the sha256 in the manifest before the plugin is told where it is, and it is cached and re-verified on every later run. The download is announced at install and again before it happens; `ROM_HUB_NO_ASSET_FETCH=1` refuses it and `rom-hub plugin assets openvgdb --fetch` performs it deliberately. `db_path` still overrides everything, for a copy you already have or one you pinned yourself. The database is opened read-only through a `file:...?mode=ro` URI, because `sqlite3.connect` on a plain path creates an empty database and a typo would otherwise answer \"no match\" forever — and because a plugin that wrote to the Hub's cached copy would fail its own integrity check next run. Matching is by hash, serial, or an exact filename — never a prefix — always scoped to one system; two matches refuse rather than choose. Covers are probed before they are proposed, so a 403 on an image costs the art and not the title. Sets no provider ids: OpenVGDB's row numbers are meaningful to nothing outside your own copy. The data stops at 2021 and at OpenEmu's 43 consoles, so there is no DOS, no Amiga and nothing after the PSP. Expect titles more often than covers: measured with the Hub's own user agent, `gamefaqs.gamespot.com` (31,235 of the 43,801 covers) answers 403 behind Cloudflare while `raw.githubusercontent.com` (10,777, all arcade) answers 200 — robots permits the fetch, the site declines to serve it, and the probe turns that into a missing cover rather than a failed enrich."
    },
    {
      "slug": "retroachievements",
      "name": "RetroAchievements",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-retroachievements",
      "install": "https://github.com/BlizzHacker/rom-hub-retroachievements",
      "download": "https://github.com/BlizzHacker/rom-hub-retroachievements/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "metadata"
      ],
      "network": [
        "retroachievements.org"
      ],
      "status": "caveat",
      "search_only": false,
      "key_required": true,
      "in_tree": false,
      "description": "Adds RetroAchievements set data to a ROM, keyed by the game's hash. Needs your RetroAchievements API key — and, separately, needs your RomM server to hold one of its own before it will accept the result.",
      "terms": "An account-gated API used as its operator intends. RetroAchievements issues every registered user a Web API key and documents this API for third-party clients, so using it with your own key is sanctioned — but it is your key and your account's rate limit, and the terms are between you and RetroAchievements. The data returned (set names, achievement counts) is community-authored and remains theirs. The plugin proposes no artwork and does not touch RA's media host.",
      "comments": "**Live-verified 2026-07-31**, with a real key, against the real API: a Mega Drive ROM imported through `libretro-content` got RomM's `md5_hash = 30c01f5a82b51fd7e23315dd070d5818`, `API_GetGameList.php?i=1` carried that exact digest on game 17392 `~Homebrew~ Alter Ego`, and the enrich wrote `ra_id = 17392` and that title — read back afterwards from RomM's own `GET /api/roms/{id}`. **Your RomM needs its own RetroAchievements key too, and this is the part that bites**: RomM 4.9.2's `PUT /api/roms/{id}` treats an incoming `ra_id` as a request to re-identify the ROM and calls RA itself, signed with RomM's `RETROACHIEVEMENTS_API_KEY`; unset, that is a `TypeError` inside yarl and a bare HTTP 500 that names nothing. `ra_id` is the only id this plugin writes, so without that variable set on the RomM container every correct match still fails at the write. Hash matching is per-dump, not per-game: the NES build of the same game is RA 8170, and neither libretro's whole-file md5 nor its header-skipped md5 appears anywhere in RA's 1,116 NES games — console 7 is not a whole-file console *and* it is a different dump. The plugin refuses rather than guessing and says which of the two you are in. On keys: the key is per-account, read-only and resettable; rotate it if the machine changes hands. **Note the published `v0.1.0` tag — the one this entry installs — still declares `api_key` as a plain `str`, so it is stored in the clear in `state.json`.** The `type = \"secret\"` handling described in the plugin's README is implemented and verified but is not in a tagged release yet; until one ships, treat the key on disk as plaintext."
    },
    {
      "slug": "retroarch-autoconfig",
      "name": "RetroArch controller profiles",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-retroarch-autoconfig",
      "install": "https://github.com/BlizzHacker/rom-hub-retroarch-autoconfig",
      "download": "https://github.com/BlizzHacker/rom-hub-retroarch-autoconfig/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "assets"
      ],
      "network": [
        "api.github.com",
        "raw.githubusercontent.com"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "RetroArch controller autoconfiguration profiles, so a gamepad your emulator does not recognise gets a mapping somebody already wrote.",
      "terms": "MIT, and this one needed checking rather than reading off a badge: GitHub reports retroarch-joypad-autoconfig as NOASSERTION, which invites the assumption that its terms are unclear. They are not. Its COPYING contains two licences, which is what defeats GitHub's single-licence detection — the MIT License (copyright 2019 The RetroArch team) covering the .cfg profiles this plugin offers, and the zlib-style SDL licence (copyright 1997-2025 Sam Lantinga) covering the bundled SDL gamecontrollerdb.cfg, which this plugin does not offer. Both halves were read from the repository itself. Both hosts are public and unauthenticated and this plugin sends no credential.",
      "comments": "The smallest source in this set and the one that solves a whole problem: a pad RetroArch does not recognise is a pad you remap by hand, and the fix is a 1.7 KB text file that already exists — 437 of them for the udev driver alone. The input driver is config and is never detected from the host OS, for the reason libretro-cores does not detect its build target: the Hub is routinely not running on the machine the pad is plugged into, and udev profiles handed to a Windows frontend are silently useless. An unknown driver is refused by name against the list that exists, before any request goes out. This plugin's repository is only 2.6 MB, so the Trees-API listing here is not strictly necessary — it is the same mechanism the overlays and cheats plugins need against 29 MB and 795 MB, kept identical so there is one way this plugin family reads a GitHub tree rather than three."
    },
    {
      "slug": "scummvm-freeware",
      "name": "ScummVM freeware games",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-scummvm-freeware",
      "install": "https://github.com/BlizzHacker/rom-hub-scummvm-freeware",
      "download": "https://github.com/BlizzHacker/rom-hub-scummvm-freeware/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer"
      ],
      "network": [
        "downloads.scummvm.org"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Twelve complete adventure games the ScummVM project distributes free, because their rights holders released them.",
      "terms": "The cleanest terms in this directory, and each one is attributable to a named person or company. Revolution Software released *Beneath a Steel Sky* and *Lure of the Temptress* as freeware in 2003; John Passfield and Steve Stamatiadis released *Flight of the Amazon Queen* in 2004; Neil Dodwell and David Dew of Creative Reality released *DreamWeb* in 2012; Alcachofa Soft released *Dráscula*; L.K. Avalon released *Sołtys* and *Sfinx*; Dynabyte released *Nippon Safes, Inc.*; Ken and Roberta Williams placed *Mystery House* in the public domain in 1987. *Broken Sword 2.5*, *The Griffon Legend* and *God of Thunder* are free releases by their own authors. Nothing here is abandonware and nothing is a dump of retail media — it is ScummVM-ready game data the project has hosted for years. On robots: `downloads.scummvm.org`, the only host this plugin requests, publishes **no** robots.txt (HTTP 404 for /robots.txt). `www.scummvm.org` is a different host whose robots.txt does `Disallow: /frs`, and this plugin never requests anything from it — it is absent from the manifest allowlist, so the broker would refuse. The twelve-game table was read once by a human from that site's robots-permitted `/games/` page and checked in; it is not fetched at runtime.",
      "comments": "The game table is the safety model, not a convenience. `/frs/extras/` is one directory per title and only some of those titles are free games: `Blade Runner/` holds subtitles, `Toonstruck/` holds cutscene subtitles, `Elvira 2/` holds sound samples, `Broken Sword I and II/` holds a subtitle pack — all add-ons for games still on sale. So the twelve directories are an **allowlist**: a directory not in it is unreachable, and no code path can build a URL into it. `tests/fixtures/scummvm_freeware/blade_runner.html` is a real capture of one of those directories, checked in so a test can prove it. Filenames, by contrast, are read live rather than tabled, so a ScummVM re-release (`drascula-int-1.0.zip` became `1.1`) keeps working with no code change; the importer re-reads the listing, requires an exact match, and lists what the directory offers now when it refuses. Checksum sidecars and manuals are excluded by name; language and audio packs are kept, because unlike a manual they are part of playing the game. Each game states its own RomM platform — all twelve are `scummvm` today — rather than sharing a module constant, so a row added later for a DOS-only drop can say so without anyone noticing that a default stopped being true. Watch the directory names: Dráscula's is `Drascula_ The Vampire Strikes Back`, with an underscore where the title has a colon, and the server 404s a near miss rather than redirecting."
    },
    {
      "slug": "universal-db",
      "name": "Universal-DB (3DS/DS homebrew)",
      "author": "BlizzHacker",
      "repository": "https://github.com/BlizzHacker/rom-hub-universal-db",
      "install": "https://github.com/BlizzHacker/rom-hub-universal-db",
      "download": "https://github.com/BlizzHacker/rom-hub-universal-db/archive/refs/tags/v0.1.0.tar.gz",
      "version": "0.1.0",
      "ref": "v0.1.0",
      "updated": "2026-07-29",
      "rpp_version": "1",
      "capabilities": [
        "search",
        "importer"
      ],
      "network": [
        "db.universal-team.net",
        "github.com",
        "*.githubusercontent.com",
        "gitlab.com",
        "codeberg.org",
        "gbatemp.net",
        "cdn.classicube.net",
        "apotrisstorage.blob.core.windows.net"
      ],
      "status": "ok",
      "search_only": false,
      "key_required": false,
      "in_tree": false,
      "description": "Searches Universal-Team's database of Nintendo 3DS and Nintendo DS homebrew and imports the release file its author published.",
      "terms": "Homebrew distributed by its own authors, which is the same basis as the homebrew (gbdev) and demozoo plugins. Universal-DB hosts nothing: every entry points at wherever the author already publishes, so an import fetches the author's file from the author's host and nothing is mirrored or re-hosted. The database itself is an open, reviewable repository (Universal-Team/db, GPL-3.0) whose entries arrive as pull requests. 270 of the 400 live entries record a licence -- GPL-3.0 (117), MIT (78), GPL-2.0 (23), Apache-2.0, MPL-2.0, BSD, Unlicense, CC0, zlib and \"other\" -- and every search row carries it. The other 130 state none; those read \"unstated\", which is a fact about the database and not a synonym for public domain, and `require_license` restricts the plugin to the ones that state terms. Nothing here is sourced from hShop or Myrient: neither appears anywhere in the data, and the manifest allowlist is the eight hosts the data actually resolves to. db.universal-team.net serves no robots.txt (404), so there is no crawl directive to observe, and the plugin makes one request per command because the database is one document.",
      "comments": "DS and 3DS are two consoles and never collapse into one another. The eight entries published for both ship a different file per console, so search returns one row per console and importing one without --platform is refused rather than guessed. Payload choice runs on the database's own evidence and nothing else: a bare .cia or .nds is a title by definition (.cia over .3dsx, .nds over .dsi, larger build wins within a format), and an archive is taken only when the entry's own `archive` manifest -- whose keys are regexes, not literals -- says a title for that console is inside it. Where the evidence runs out the import refuses and names the files it saw: 61 of 394 entry-platform pairs, including SuDokuL (whose five bare zips include a GameCube and an x64 Windows build) and TheXTech (whose one pattern matches its program and both 48 MB asset packs). Those entries still appear in search. Three of the nine categories never appear at all -- `plugin` (Luma .3gx, injected into a commercial game), `firm` (boot-chain firmware) and `exploit` -- because none of them boots as a title. Two entries publish over plain http and are refused by name, because netpolicy permits https only and this does not work around it. `*.githubusercontent.com` is declared because a GitHub release URL is a 302 to release-assets.githubusercontent.com and the broker re-checks every hop."
    }
  ]
}
